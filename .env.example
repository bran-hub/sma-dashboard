# Optional local overrides. Keep real secrets out of git.
SMA_DASHBOARD_DB=data/db/sma_dashboard.db
SMA_ENABLE_CHAT=0
SMA_CHAT_MODE=mock

# For live assistant mode, install `.[chatbot]`, set SMA_CHAT_MODE=anthropic,
# and put ANTHROPIC_API_KEY in the ignored .streamlit/secrets.toml file.
